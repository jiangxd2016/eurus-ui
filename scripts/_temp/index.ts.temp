import type { App } from 'vue';
import <%= prefixName %> from './src';

<%= prefixName %>.install = (app: App) => {
  app.component(<%= prefixName %>.name, <%= prefixName %>);
};

export { <%= prefixName %> };
export default <%= prefixName %>;

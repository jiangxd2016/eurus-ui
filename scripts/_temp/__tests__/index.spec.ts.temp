import { mount } from '@vue/test-utils';
import <%= prefixName %> from '..';

describe('<%= name %>', () => {

 it('<%= name %> snapshot', () => {
    const wrapper = mount(<%= prefixName %>);
    expect(wrapper.html()).toMatchSnapshot();
    wrapper.unmount();
  });

});

import { mount } from '@vue/test-utils';
import <%= name %> from '../';

describe('<%= name %>', () => {

  test('<%= name %> snapshot', () => {
    const wrapper = mount(<%= name %>);
    expect(wrapper.html()).toMatchSnapshot();
    wrapper.unmount();
  });
});

import { defineComponent } from 'vue';
import './style.scss';

const <%= prefixName %>Props = {
  type: {
    type: Boolean,
    default: false,
  },
};

export default defineComponent({
  name: '<%= prefixName %>',
  props: <%= prefixName %>Props,
  setup(props, { slots, emit }) {

    return () => (
      <div>

      </div>

    );
  },
});
